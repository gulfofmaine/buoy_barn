ignore-patterns:
  - "(^|/)migrations(/|$)"

# doc-warnings: true

uses:
  - django

pylint:
  disable:
    - logging-fstring-interpolation
    - import-error
pep257:
  disable:
    - D203
    - D213
# mypy:
#   run: true
# vulture:
#   run: true
